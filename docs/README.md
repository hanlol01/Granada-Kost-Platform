# Granada Kost Platform Documentation

Indeks dokumentasi proyek. Untuk status milestone terbaru (M12 File Upload Foundation dan seterusnya), sumber kebenaran adalah dokumen implementasi di `12-product-readiness/` dan ADR di `01-architecture/` — bukan ringkasan high-level.

## Struktur Direktori

| Direktori | Isi |
| --- | --- |
| `00-project/` | Project governance: `ROADMAP.md`, `CHANGELOG.md`, `BACKLOG.md`, `PROJECT_MASTER.md`, `PROJECT_HEALTH_REVIEW_V1.md`, `INTERNAL_DEMO_CHECKLIST.md`, `PROJECT_HANDOFF.md`, `DEVELOPMENT_WORKFLOW.md`, `AGENTS.md` |
| `01-architecture/` | Arsitektur global: `BACKEND_ARCHITECTURE.md`, `API_PLANNING.md`, `DATABASE_PLANNING.md`, `DOMAIN_MODEL.md`, `SECURITY_POLICY.md`, `ARCHITECTURE_DECISIONS.md`, `FRONTEND_ARCHITECTURE_DECISIONS.md` (ADR-FE-001..011, frozen M11AF), `ADR-BE-FILE-001_BACKEND_MEDIATED_FILE_ACCESS.md` |
| `02-domains/` | Business domains: Billing, Complaint, Vehicle, Notification, Smart Lock, CCTV |
| `03-database/` | Database planning per domain |
| `04-smartlock/` | Smart Lock architecture, policy, business guide, Tuya compatibility audit |
| `05-master-data/` | Master data dan seed plan |
| `06-analysis/` | Research dan gap analysis |
| `07-reports/` | Laporan mingguan untuk stakeholder |
| `09-progress/` | Progress mingguan internal (`Week_1_Kostation.md`, `Week_2_Kostation.md`, `Week_3_Kostation.md`) |
| `10-frontend/` | Frontend integration plan (M11) |
| `11-qa/` | QA dan bug triage |
| `12-product-readiness/` | Product readiness dan File Upload Foundation (M12) |
| `13-smart-lock/` | Smart Lock live Tuya/PALOMA readiness (M13): `SMART_LOCK_TUYA_SITE_READINESS_PLAN.md` (M13A site readiness plan; legacy PoC `reference/tuya-paloma-poc/` diaudit sebagai referensi saja), `SMART_LOCK_LIVE_INTEGRATION_ARCHITECTURE_FREEZE.md` (M13B architecture freeze — binding untuk M13C–M13H), `SMART_LOCK_TUYA_PROVIDER_CONFIG_CLIENT_IMPLEMENTATION.md` (M13C provider config + client skeleton — live commands tetap disabled) |
| `14-production-readiness/` | Production readiness / internal demo refresh (M14): `PRODUCTION_READINESS_AUDIT.md` (M14A audit — readiness levels pasca M12+M13, feature readiness matrix, klarifikasi status Smart Lock — execution pending, production blockers, scope demo internal, QA track M14B–M14F, deployment/env checklist, documentation gaps, risk register; dokumentasi saja, tanpa validasi terminal), `API_REGRESSION_SECURITY_SMOKE.md` (M14B — API regression & security smoke, verdict PASS, dieksekusi eksternal), `BROWSER_REGRESSION_INTERNAL_DEMO_FLOW.md` (M14C — browser regression / internal demo flow, verdict PASS, Hybrid Interactive Login, dieksekusi eksternal), `INTERNAL_DEMO_SCRIPT_REFRESH.md` (M14D — skrip demo internal yang aman dan repeatable: prinsip demo, pre-demo checklist, alur demo bersegmen + naskah presenter Bahasa Indonesia, batasan kata Smart Lock — live execution tetap NO-GO, fallback plan, referensi artefak; dokumentasi saja, tanpa validasi terminal), `RELEASE_READINESS_VERDICT.md` (M14F — verdict akhir track M14: internal demo READY, production NOT READY, Smart Lock live execution pending/NO-GO, decision matrix, pre-production + site-trial checklist, next milestones; dokumentasi saja, tanpa validasi terminal) |
| `15a-stakeholder-demo/` | Stakeholder demo delivery (M15): `INTERNAL_DEMO_DELIVERY_PACKAGE.md` (M15A — paket delivery demo internal + review stakeholder: tujuan demo, scope aman, agenda 30–45 menit, presenter checklist, form feedback stakeholder, template decision log, wording Smart Lock yang aman, referensi evidensi, post-demo actions, risk/fallback; dokumentasi saja, tanpa validasi terminal) |
| `15b-deployment/` | Deployment and staging evidence (M15): `VPS_STAGING_BASELINE_SMOKE_ENV_HARDENING.md` (M15B-A — VPS staging baseline smoke + environment hardening evidence: service/Nginx/TLS/CORS checks, Smart Lock simulated/live disabled, Payment Gateway disabled, Midtrans sandbox-only, staging PASS; production tetap NOT READY) |
| `15c-payment-gateway/` | Payment Gateway (M15C): `PAYMENT_GATEWAY_ARCHITECTURE_FREEZE.md` (M15C-A — architecture/product freeze), `MIDTRANS_PROVIDER_CONTRACT_FREEZE.md` (M15C-B — provider contract freeze), `BACKEND_PAYMENT_GATEWAY_FOUNDATION.md` (M15C-C — backend foundation), `MIDTRANS_SANDBOX_SNAP_WEBHOOK_SETTLEMENT.md` (M15C-D — backend Midtrans Sandbox Snap + signed webhook settlement implementation; no frontend UI; not production-ready), `FRONTEND_PAYMENT_CTA_ADMIN_STATUS_UI_PLAN.md` (M15C-E1 — frontend UX/UI implementation plan untuk M15C-E2: state machine Penghuni A–F, CTA "Bayar Online" + fallback bukti manual, perilaku buka Snap/paymentUrl, polling status, tabel/detail transaksi Admin + badge Gateway/Manual, copy Bahasa Indonesia, mapping error, mapping implementasi, QA plan M15C-F; dokumentasi saja, tanpa implementasi kode, not production-ready), `FRONTEND_PAYMENT_CTA_ADMIN_STATUS_UI_IMPLEMENTATION.md` (M15C-E2A/E2B — implementasi frontend + validasi teknis: Penghuni CTA "Bayar Online" + state pending/lunas/gagal/tinjauan + polling terbatas + fallback bukti manual dipertahankan; Admin tab "Online" tabel/detail transaksi gateway + badge Gateway/"Terkonfirmasi Otomatis"/"Perlu Tinjauan"; lint/typecheck/build dan API smoke PASS; tanpa secret di frontend; not production-ready), `PAYMENT_GATEWAY_SANDBOX_E2E_QA.md` (M15C-F/F2 — QA sandbox staging PASS: freshness/static/health/build/leakage PASS, Hybrid Manual Browser QA PASS, Midtrans Sandbox Snap opened from UI, signed webhook settlement + duplicate/idempotency PASS; Payment Gateway tetap sandbox/staging dan not production-ready), `PAYMENT_GATEWAY_RELEASE_UPDATE.md` (M15C-G — release/documentation update penutup track M15C: Payment Gateway sandbox/staging ready, Midtrans Sandbox validated, production payment activation pending, kompatibilitas manual proof, security boundaries, evidence references; not production-ready) |
| `16-room-inventory-booking/` | Room Inventory & Public Booking (M16): `ROOM_INVENTORY_PUBLIC_BOOKING_ARCHITECTURE_FREEZE.md` (M16A — architecture/UX freeze untuk redesign Admin Room Management + Public Room Listing / WhatsApp Booking MVP: navigasi Admin "Kamar" dropdown, Ringkasan Kamar, halaman Rumah Kost/Apart Kost, Unit Bangunan, status model, aturan public listing per gender, keputusan agregasi tanpa nomor kamar publik, template WhatsApp booking, data model + API plan, import strategy staging-only, milestone M16B–M16H; dokumentasi saja — tanpa kode/migrasi/seed/QA; public booking NOT production-ready), `ROOM_SCHEMA_AUDIT_IMPORT_MAPPING.md` (M16B-1 — audit schema room backend existing + import mapping: audit `rooms`/`room_types`/occupancy/billing dependencies dari migration 002/004 + module room + seed deterministic, temuan gender assignment seed stale vs data M16A-0, mapping legacy `number` ↔ `room_code` yang mempertahankan room UUID, status mapping (booked≡reserved; requires_review butuh CHECK tambahan), strategi upsert idempoten dry-run-first staging-only, rekomendasi Option B (`room_buildings` + extend `rooms` additif), 16 validator check untuk M16B-3; dokumentasi saja — tanpa kode/migrasi/import/SQL), `BACKEND_ROOM_MODEL_MIGRATION_DESIGN.md` (M16B-2 — desain model backend + migrasi additive Room Inventory: tabel `room_buildings` baru, ekstensi `rooms` nullable-first (`building_id`, `room_code` partial-unique, `category`, `floor_code/label`, `public_visible`, `yearly_price`, kolom `import_*`), status CHECK ditambah `requires_review` (booked→reserved untuk MVP), mapping putra→male/putri→female, harga building=default/room=operatif, strategi backfill deterministic yang mempertahankan `rooms.id`, idempotency/rollback, 6 fase migrasi + gate validasi, SQL outline design-only; dokumentasi saja — tanpa file migrasi/kode/import/SQL) |
| `17-booking-leads/` | Booking Lead MVP (M17): `BOOKING_LEAD_MVP_ARCHITECTURE_FREEZE.md` (M17A - architecture/UX/safety freeze untuk Booking Lead MVP; frozen, mengikat untuk M17B+; dokumentasi saja - tanpa kode/migrasi/seed/QA; public booking NOT production-ready), `BOOKING_LEAD_BACKEND_API.md` (M17B - backend-only additive booking lead foundation: `booking_leads` migration, public write-only endpoint, authenticated admin list/status endpoints, validation PASS; no reservation/invoice/occupancy/payment/Smart Lock changes; public booking tetap NOT production-ready), `BOOKING_LEAD_ADMIN_UI.md` (M17C - Admin Booking Lead Management UI: route `/booking-leads` + nav "Minat Booking" (manager/admin), list/filter/status update/WhatsApp follow-up di atas API M17B; tanpa reservasi/invoice/occupancy/resident otomatis, tanpa Payment Gateway/Smart Lock; public booking tetap NOT production-ready), `BOOKING_LEAD_ADMIN_UI_QA.md` (M17C-QA - admin lint/typecheck/build PASS, API lint/build PASS, backend smoke PASS termasuk admin 200, invalid status 400, unauth 401, property-owner 403; browser visual tidak dilakukan pada pass ini), `BOOKING_LEAD_PUBLIC_FORM_UI.md` (M17D - public /kamar "Ajukan Minat Booking" lead form UI di atas endpoint publik M17B; anonymous POST tanpa auth header/refresh-token, tanpa perubahan ApiClient; WhatsApp CTA tetap; tanpa reservasi/invoice/occupancy/resident otomatis; public booking tetap NOT production-ready), `BOOKING_LEAD_PUBLIC_FORM_UI_QA.md` (M17D-QA - penghuni lint/typecheck/build PASS, API build PASS, public booking lead API smoke PASS termasuk duplicate protection dan invalid payload 400; browser visual tidak dilakukan pada pass ini), `M17_BOOKING_LEAD_FINAL_RELEASE_HANDOFF.md` (M17E - final release/handoff penutup track M17: ringkasan milestone, kapabilitas, alur end-to-end, validasi, limitasi browser, safety/privacy, deployment/config, deferred, rekomendasi M18; Booking Lead MVP READY untuk internal/demo/staging dengan konfirmasi admin/manual; public booking tetap NOT production-ready) |
| `18-public-hunian-catalog/` | Public Hunian Catalog / Modern Listing (M18): `PUBLIC_HUNIAN_CATALOG_CONTENT_UX_FREEZE.md` (M18A - content/UX freeze untuk katalog publik modern: model produk "Hunian Catalog Item" (public-safe, level hunian/unit/grup - tanpa nomor kamar eksak/roomId/room_code), arah UI modern ala hotel/apartemen, struktur halaman listing `/kamar` + detail `/kamar/$slug`, strategi konten placeholder-first, posture booking/payment (lead + WhatsApp; payment booking DEFERRED), safety/privacy mengikat, deferred scope, milestone M18B-M18G; dokumentasi saja - tanpa kode/migrasi/QA; public booking tetap NOT production-ready), `PUBLIC_HUNIAN_CATALOG_API.md` (M18B - backend/public hunian catalog API: `GET /api/v1/public/hunian-catalog` + detail slug, memakai M16 availability + M18 public-safe content constants, `bookingLeadDefaults` kompatibel M17, tanpa migration/frontend/gallery upload/payment/Smart Lock; lint/build/API smoke/safety scan PASS) |

M18A public hunian catalog content/UX freeze: `18-public-hunian-catalog/PUBLIC_HUNIAN_CATALOG_CONTENT_UX_FREEZE.md` freezes the product, content, and UX direction for the M18 modern public catalog on top of the completed M16/M17 base. Frozen decisions: the public catalog displays **hunian/unit/group offerings, never exact rooms** (e.g. "Rumah Kost Putra - Unit 01"); a public-safe **Hunian Catalog Item** model (slug, title, category/gender + labels, safe building/floor labels, short/long description, price from monthly/yearly, availability count, facilitiesRoom/facilitiesShared, policies/rules, gallery references if safe, FAQ, ctaLabel, bookingLeadDefaults mapping 1:1 to the existing M17 lead payload; explicitly excluding exact room numbers, room IDs, `room_code`, resident/occupancy/payment/Smart Lock data); modern hotel/apartment-style, mobile-first, non-dashboard UI direction with hero, filter chips, polished cards, facility badges, price hierarchy, and full loading/empty/error states; listing `/kamar` card structure (title, badges, short description, price from, availability, 3-5 facility badges, CTA Lihat Detail + Ajukan Minat Booking + WhatsApp) and detail `/kamar/$slug` structure (gallery, price, availability, descriptions, fasilitas kamar/bersama, aturan/kebijakan, FAQ, sticky CTAs, disclaimer "Ketersediaan dan nomor kamar dikonfirmasi oleh admin. Pengajuan minat booking belum menjadi booking resmi."); placeholder-first content strategy (M18A freezes schema; M18B may ship with placeholders; master data enrichment later; no invented facility claims); booking/payment posture unchanged (lead primary, WhatsApp confirmation, payment booking DEFERRED, no auto reservation, no exact room selection, admin confirmation source of truth); binding safety/privacy rules; deferred scope (checkout, room hold, auto invoice, resident auto-creation, user accounts, full media management, advanced SEO, reviews, maps); milestones M18B-M18G. Documentation/freeze only; no application source code changes; Claude Fable 5 ran no lint/build/API/browser validation; public booking remains NOT production-ready.

M17E final booking lead release/handoff: `17-booking-leads/M17_BOOKING_LEAD_FINAL_RELEASE_HANDOFF.md` closes the M17 Booking Lead MVP track with the consolidated milestone summary (M17A architecture/UX/safety freeze - lead is NOT a confirmed booking, no auto reservation/payment/invoice/occupancy/resident, no exact room selection, WhatsApp/admin confirmation required, minimum admin-only PII; M17B backend API with additive `booking_leads` migration, public write-only rate-limited duplicate-protected endpoint, admin RBAC/property-scoped list/status endpoints, masked/PII-safe audit, validation PASS; M17C Admin lead inbox `/booking-leads` + "Minat Booking" nav with QA PASS with browser limitation; M17D public `/kamar` "Ajukan Minat Booking" form with anonymous POST, not-a-confirmed-booking success state, WhatsApp follow-up, payload excluding roomId/roomCode/exact room numbers/propertyId, QA PASS with browser limitation), delivered capabilities, end-to-end flow, validation summary, accepted browser limitation notes, safety/privacy summary, deployment/config notes (migration 014, Redis rate limit, no new env vars), deferred scope, and the release verdict: internal/demo/staging **READY with known limitations**; production public booking **NOT READY**; Booking Lead MVP **READY for internal/demo/staging use with admin/manual confirmation**; payment booking **DEFERRED**; Smart Lock live command **NO-GO until site trial/evidence/signoff**. Recommended next milestone: **M18 - Public Hunian Catalog Detail / Modern Listing** (hunian/unit-level detail without exact room numbers, photos/gallery, facilities, policies, pricing explanation, FAQ, SEO, WhatsApp/lead CTA; no payment gateway booking). Documentation only; Claude Fable 5 ran no lint/build/API/browser validation.

M17D public /kamar booking lead form UI: `17-booking-leads/BOOKING_LEAD_PUBLIC_FORM_UI.md` documents the public lead form added to the unauthenticated `/kamar` page: each availability card gains a primary "Ajukan Minat Booking" CTA (the WhatsApp CTA "Tanya Ketersediaan via WhatsApp" remains as secondary action and primary confirmation channel) opening a dialog with a read-only public-safe group summary, fields Nama Lengkap + Nomor WhatsApp (required) and Tanggal Rencana Masuk + Catatan untuk Admin (optional), a honeypot anti-spam field, and submission via anonymous `POST /api/v1/public/booking-leads` (no Authorization header, no refresh-token flow; no ApiClient change was needed because `anonymous: true` is already supported for POST). Success state "Minat booking berhasil dikirim." clarifies the lead is not a confirmed booking and offers "Hubungi Admin via WhatsApp" (via `VITE_PUBLIC_WHATSAPP_NUMBER`) plus "Lihat Kamar Lain"; errors use safe generic/rate-limit copy only. No room IDs/room_code/exact room numbers sent or rendered, no payment/invoice/occupancy/resident automation, no Payment Gateway or Smart Lock changes. Milestone label drift vs the M17A freeze (public form planned as M17E) is documented. `BOOKING_LEAD_PUBLIC_FORM_UI_QA.md` records M17D-QA PASS with browser limitation. Public booking remains not production-ready.

M17C admin booking lead management UI: `17-booking-leads/BOOKING_LEAD_ADMIN_UI.md` documents the authenticated Admin lead inbox at `/booking-leads` (sidebar "Minat Booking", roles manager/admin per the M17B authorization; property-scoped) built on the M17B endpoints: lead table + mobile list showing visitor name/phone/message, category/gender labels (Rumah Kost/Apart Kost, Putra/Putri), building/floor codes, preferred move-in date, status, source, and created/updated timestamps; status/category/gender selects plus name/phone search; guarded status transitions mirroring the backend state machine (Baru -> Sudah Dihubungi -> Jadwal Survey -> Dikonversi, plus Ditolak/Kedaluwarsa; terminal states locked; "Dikonversi" is a manual marker only - no resident/occupancy/invoice/reservation) via `PATCH /booking-leads/:id/status` with confirm dialogs and `room.manage` gating; "Hubungi via WhatsApp" wa.me follow-up with a public-safe prefilled template (no exact room numbers/IDs); loading/empty/error states. No payment/invoice/occupancy/resident automation, no room reservation, no Payment Gateway or Smart Lock changes; `BOOKING_LEAD_ADMIN_UI_QA.md` records M17C-QA PASS with browser limitation. Public booking remains not production-ready.

M17A booking lead MVP architecture freeze: `17-booking-leads/BOOKING_LEAD_MVP_ARCHITECTURE_FREEZE.md` freezes the product, data, UX, API, and safety/privacy direction for the Booking Lead MVP (deferred from the M16A freeze plan). Frozen decisions: a booking lead is NOT a confirmed booking and never reserves a room, creates an invoice/occupancy, or involves payment; `booking_leads` conceptual model (property-scoped, aggregated interest via category/gender/building/floor/public_group_key, minimum PII: visitor_name + normalized visitor_phone + optional message/move-in date, status new/contacted/visit_scheduled/converted/rejected/expired, source `public_kamar`, safe metadata JSON; explicitly excluding exact room numbers, payment fields, invoice_id, occupancy_id, Smart Lock data, and identity documents); public flow on `/kamar` via "Ajukan Minat Booking" form alongside the unchanged primary WhatsApp CTA; admin lead inbox with status workflow and manual-only conversion; API concept `POST /api/v1/public/booking-leads` (write-only, rate-limited, duplicate protection, no auth, no reservation) plus authenticated property-scoped `GET /api/v1/booking-leads` and `PATCH /api/v1/booking-leads/:id/status` with audit; binding safety/PII rules (rate limits, phone validation, spam rejection, message sanitization, minimum PII, no identity documents, PII never in logs/public responses, retention/cleanup recommendation); deferred scope (online booking payment, reservation automation, exact room selection, resident auto-creation, document upload, staff assignment, notification automation, CRM features); implementation milestones M17B-M17G. Documentation/freeze only; no code, no migrations, no seed, no QA executed; Claude Fable 5 ran no lint/build/API/browser validation; Payment Gateway and Smart Lock postures unchanged; public booking remains NOT production-ready.

M16C admin room management redesign: `16-room-inventory-booking/ADMIN_ROOM_MANAGEMENT_REDESIGN.md` documents the Admin Kamar redesign on top of the M16B backfilled inventory: in-page tabs for Ringkasan, Rumah Kost, Apart Kost, and Ketersediaan; summary cards; category filters and building/floor grouping; lightweight availability view with booking leads deferred; additive admin rooms API fields while preserving legacy keys; validation PASS for API lint/build, Admin lint/typecheck/build, health smoke, authenticated rooms smoke with 163 rooms, and temporary built API smoke with inventory keys. No CSV import/backfill, no DB mutation, no tenant PII exposure, and no Payment Gateway or Smart Lock behavior changes; public booking remains not production-ready.

M16C-QA admin room management browser smoke: `16-room-inventory-booking/ADMIN_ROOM_MANAGEMENT_BROWSER_QA.md` records the QA-only follow-up for Admin Kamar. Verdict PARTIAL: API health, authenticated rooms, fresh built API inventory fields, API lint/build, and Admin lint/typecheck/build passed; browser visual smoke, screenshots, console inspection, and browser network inspection were blocked because no Chromium, Firefox, Playwright, or Puppeteer tooling was available. No DB mutation, no CSV import/backfill, no Payment Gateway or Smart Lock changes; public booking remains not production-ready.

M16D public room listing API: `16-room-inventory-booking/PUBLIC_ROOM_LISTING_API.md` documents the backend-only public-safe room availability API for future public listing UI. Verdict PASS: API lint/build and endpoint smoke passed; implementation adds unauthenticated read-only aggregated availability, summary, and safe group-detail endpoints, enforces backend gender/category/floor filters, excludes exact room numbers and tenant/resident/occupancy PII, and keeps Payment Gateway plus Smart Lock untouched. Public frontend UI, WhatsApp CTA UI, booking leads, online booking/payment, and production readiness remain deferred.
M16E-QA public room listing UI validation: `16-room-inventory-booking/PUBLIC_ROOM_LISTING_UI_QA.md` records validation for the public `/kamar` listing and WhatsApp CTA. Verdict PARTIAL: Penghuni typecheck/build, API build, fresh M16D API smoke, static route/public-access checks, WhatsApp CTA static checks, and privacy scan passed; Penghuni lint failed due M16E `kamar.tsx` formatter issues plus unrelated billing/payment formatter errors, current branch was `master` instead of `m16e-public-room-listing-ui`, and browser visual/no-login QA was not executed because browser tooling was unavailable. No DB mutation, no CSV import/backfill, no Payment Gateway or Smart Lock changes; public booking remains not production-ready.

M16E public room listing UI + WhatsApp CTA: `16-room-inventory-booking/PUBLIC_ROOM_LISTING_UI_WHATSAPP_CTA.md` documents the unauthenticated public listing page at `/kamar` in `apps/penghuni` (registered in `PUBLIC_ROUTES`, outside the AuthGuard): hero "Temukan Kamar Kost yang Sesuai" with Putra/Putri gender filter and Semua/Rumah Kost/Apart Kost category filter (no mandatory popup, shareable `?gender=` URLs), aggregated availability cards consuming the M16D public API anonymously (publicTitle, category/gender labels, building/floor labels, availableCount, monthly/yearly from-prices), WhatsApp CTA "Tanya Ketersediaan via WhatsApp" with the frozen prefilled template using `VITE_PUBLIC_WHATSAPP_NUMBER` (missing env renders a safe disabled CTA "Nomor WhatsApp admin belum dikonfigurasi."), loading skeletons, empty state "Belum ada kamar tersedia untuk filter ini.", and a safe retryable error state. No room IDs/room_code/exact room numbers, no tenant/resident/occupancy PII, no booking leads, no payment gateway, no Smart Lock changes, no new workspace. Claude Fable 5 did not run lint/build/browser/API validation; validation is deferred to M16E-QA Codex. Public booking remains not production-ready.

M16F final release / handoff: `16-room-inventory-booking/M16_FINAL_RELEASE_HANDOFF.md` closes the M16 Room Inventory & Public Booking track with the consolidated milestone summary (M16A-0 normalization 163/123/40 and 99/64 PII-masked; M16A architecture/UX freeze; M16B additive schema + staging backfill with 26 `room_buildings` and 163 rooms backfilled in place, room IDs preserved; M16C tabbed Admin redesign with M16C-QA PARTIAL accepted; M16D public API validation PASS; M16E `/kamar` public UI + WhatsApp CTA with validation partial accepted), delivered capabilities, validation summary, accepted partial QA notes (browser tooling unavailable on VPS; global penghuni lint blocked by an unrelated Payment/Billing formatting baseline), safety/privacy summary, deployment/config notes including `VITE_PUBLIC_WHATSAPP_NUMBER`, deferred scope (booking leads, online booking payment, media/SEO), and the release verdict: internal/demo/staging **READY with known limitations**; production **NOT READY for full production public booking**; Smart Lock live command **NO-GO until site trial/evidence/signoff**; payment booking **DEFERRED - manual/WhatsApp confirmation remains the MVP path**. Documentation only; Claude Fable 5 ran no lint/build/API/browser validation.

M16B-4B staging backfill execution: `16-room-inventory-booking/STAGING_BACKFILL_EXECUTION.md` documents the guarded staging import run using `ROOM_INVENTORY_IMPORT_CONFIRM=APPLY_M16_ROOM_INVENTORY` and `ROOM_INVENTORY_BACKUP_CONFIRMED=true`. Result: PARTIAL overall; staging backfill PASS; 26 `room_buildings` inserted, 163 existing `rooms` updated in place, room IDs preserved, `rooms` remains 163, `rooms_with_room_code` becomes 163, duplicate `room_code` remains 0, residents/occupancies unchanged, no tenant PII introduced, health API smoke PASS. Authenticated rooms API smoke was not run due tool approval limit. No Payment Gateway or Smart Lock files changed; public booking remains not production-ready.

M16B-4A import execution script with apply guard: `16-room-inventory-booking/IMPORT_APPLY_GUARD_IMPLEMENTATION.md` documents the guarded apply path added to `backend/api/src/infrastructure/database/scripts/validate-room-inventory-import.ts` and npm command `npm --workspace @granada-kost/api run room-inventory:apply`. Default execution remains dry-run; confirmed apply requires `--apply`, `ROOM_INVENTORY_IMPORT_CONFIRM=APPLY_M16_ROOM_INVENTORY`, and `ROOM_INVENTORY_BACKUP_CONFIRMED=true`. M16B-4A validation is limited to dry-run and apply-refusal without confirmation env; no DB mutation is executed, `rooms` remains 163, `room_buildings` remains 0, `rooms_with_room_code` remains 0, no tenant PII is exposed, and Payment Gateway plus Smart Lock behavior remain unchanged. Confirmed staging import is deferred to a later approved milestone.

M16B-3B CSV validator + dry-run implementation: `16-room-inventory-booking/CSV_VALIDATOR_DRY_RUN_IMPLEMENTATION.md` documents the read-only validator/dry-run script `backend/api/src/infrastructure/database/scripts/validate-room-inventory-import.ts` and npm command `npm --workspace @granada-kost/api run room-inventory:validate`. The script validates normalized room CSVs (headers, totals, enums, prices, building/floor/gender consistency, occupancy seed masking, PII scan) and runs a SELECT-only DB comparison against the current M16B-3A schema. Latest generated report `16-room-inventory-booking/reports/ROOM_INVENTORY_DRY_RUN_REPORT_20260707_161613.md` is PASS: 26 building rows, 163 room rows, 2 sanitized occupancy rows, 0 duplicate room_code, 0 PII findings, DB pre-backfill state, 163 inferred legacy matches, 0 unmatched CSV/DB rows, proposed future 26 building inserts + 163 room updates, with manual-review items for dev occupancy/status conflicts and legacy ApartKost floor-hint mismatches. No DB writes, no seed, no backfill, no frontend/public listing, and no Payment Gateway or Smart Lock behavior changes.

M16B-3A additive room inventory migration implementation: `16-room-inventory-booking/ADDITIVE_MIGRATION_IMPLEMENTATION.md` documents the backend schema implementation and validation evidence for migration `013_room_inventory.sql`. Implemented scope: new `room_buildings` table, additive nullable-first `rooms` columns, `rooms_status_check` extended with `requires_review`, partial unique index on `(property_id, room_code) WHERE room_code IS NOT NULL`, replay-safe SQL for the repository migration runner, and staging validation (lint/build, migration apply + replay, schema inspection, health and authenticated rooms API smoke). Explicitly not done: no CSV import, no 163-room backfill, no seed execution, no frontend redesign, no public listing/booking leads, no Payment Gateway or Smart Lock behavior changes, no tenant PII exposure. Next recommended milestone: M16B-3B CSV Validator + Dry-run Import Script.

M16B-2 backend room model / migration design: `16-room-inventory-booking/BACKEND_ROOM_MODEL_MIGRATION_DESIGN.md` freezes the backend model and additive migration strategy for M16 Room Inventory (Option B from M16B-1). Frozen design: new `room_buildings` table (UNIQUE property_id+category+building_code; gender male/female only; total=floorA+floorB CHECK; building-level default prices; building `public_visible`), nullable-first `rooms` extensions (`building_id` FK RESTRICT, canonical `room_code` with partial unique index until backfill, `category`, `floor_code` A/B + `floor_label`, `public_visible` default true, `yearly_price`, `import_source/_row/_notes`), additive status CHECK extension adding `requires_review` while `booked` maps to existing `reserved` at storage level, putra→male / putri→female with the one-gender-per-building invariant enforced by validator + application logic, deterministic legacy-number→room_code backfill matching that preserves `rooms.id` and all occupancy/invoice/complaint FKs, dry-run-first idempotent no-delete import with rollback notes, six migration phases with safety gates, a design-only SQL outline (no migration file), validation gates for M16B-3/M16B-5, public listing readiness dependencies, and the recommended breakdown M16B-3 (migration, optionally 3A/3B) → M16B-4 (validator/dry-run) → M16B-5 (staging import) → M16B-6 (API contract) → M16C (Admin redesign), superseding the M16B-1 numbering. Documentation only; no code, no migration files, no imports, no SQL executed; Payment Gateway and Smart Lock statuses unchanged; public booking not production-ready; production remains NOT READY.

M16B-1 room schema audit & import mapping: `16-room-inventory-booking/ROOM_SCHEMA_AUDIT_IMPORT_MAPPING.md` audits the existing backend room model from source (migrations `002_property_room.sql`/`004_room_master_data_alignment.sql`, `modules/room`, deterministic seed `core-seed.data.ts`) and freezes the import mapping from the M16A-0 normalized CSVs to the future Room Inventory model. Key findings/decisions: existing `rooms` has `UNIQUE(property_id, number)`, a 5-value status CHECK (vacant/reserved/occupied/maintenance/inactive), `unit_code` + `gender_policy` from migration 004, and **no** buildings table/category/room_code/floor_code/public_visible/yearly_price; the current seed uses the superseded gender mapping (RuKost Putra 57/Putri 66) and legacy numbering (`RK-01-01`, `AK-05A-1B`) — a deterministic legacy↔new mapping via (category, building_code, room_number) corrects gender and preserves room UUIDs plus all occupancy/invoice/complaint FKs. Status mapping: booked≡existing reserved (presentation-level for MVP), requires_review needs an additive CHECK change. Import strategy: upsert keys (property_id, category, building_code) and (property_id, room_code); dry-run diff + conflict reports first; no destructive delete; no status overwrite on occupancy-linked rooms; idempotent; staging only. Migration recommendation: Option B — add `room_buildings` + extend `rooms` additively. Includes PII rules, compatibility analysis (Payment Gateway and Smart Lock unaffected, statuses unchanged), public listing API readiness, 16 validator checks, and the M16B-2..M16B-5 breakdown. Documentation only; no code, no migrations, no imports, no SQL executed; production remains NOT READY.

M16A room inventory & public booking architecture freeze: `16-room-inventory-booking/ROOM_INVENTORY_PUBLIC_BOOKING_ARCHITECTURE_FREEZE.md` freezes the product, data, UX, and architecture direction for the redesigned Admin Room Management and the Public Room Listing / WhatsApp Booking MVP on top of the validated M16A-0 normalized room master data (163 rooms; RuKost 123 — Putra 75/Putri 48, 16 buildings; ApartKost 40 — Putra 24/Putri 16, 10 units; 0 duplicate room_code; tenant PII masked). Frozen decisions: Admin sidebar "Kamar" dropdown (Ringkasan Kamar, Rumah Kost, Apart Kost, Ketersediaan/Booking; Unit Bangunan as a summary tab in MVP), building→floor grouped category pages, status vocabulary (vacant/occupied/booked/maintenance/requires_review), public listing shows only vacant + public_visible rooms as aggregated availability (no exact room numbers in MVP), hero gender filter (no mandatory popup), WhatsApp admin-confirmation booking (no payment gateway involvement), conceptual data model (`room_buildings`, extended `rooms`, later `room_booking_leads`), admin/public API plan with backend-enforced gender filtering and safe-field allowlist, staging-only idempotent import strategy, and milestone breakdown M16B–M16H. Documentation only; no code, no migrations, no seed, no mockup changes, no QA executed; public booking NOT production-ready; Payment Gateway and Smart Lock statuses unchanged; production remains NOT READY.

M13D implementation note: `13-smart-lock/SMART_LOCK_READ_ONLY_DIAGNOSTIC_IMPLEMENTATION.md` documents the backend read-only diagnostic/capability discovery endpoint. M13E implementation note: `13-smart-lock/SMART_LOCK_READ_ONLY_SYNC_IMPLEMENTATION.md` documents backend read-only sync persistence and gateway health updates. M13F-A safety freeze: `13-smart-lock/SMART_LOCK_CONTROLLED_LIVE_COMMAND_SAFETY_FREEZE.md` freezes the safety gates, RBAC/property-scope, idempotency, rate-limit, confirmation, audit, response contract, rollback, and site-trial rules for future controlled live commands — documentation only, binding for M13F-B onward. Live commands remain disabled.

M13F-B implementation note: `13-smart-lock/SMART_LOCK_COMMAND_GUARD_IMPLEMENTATION.md` documents the backend command guard, idempotency, rate-limit, validation, and audit behavior. Live commands remain disabled.

M13F-C1 site trial runbook: `13-smart-lock/SMART_LOCK_CONTROLLED_LIVE_COMMAND_SITE_TRIAL_RUNBOOK.md` defines the readiness checklists (approvals, credentials, device mapping, backend dry-run), the dry-run sequence, the future controlled live trial sequence, rollback/emergency procedure, evidence template, Go/No-Go conditions, and the M13F-C2 implementation contract — documentation only. Live commands remain disabled and are not implemented.

M13F-C2 implementation note: `13-smart-lock/SMART_LOCK_LIVE_UNLOCK_TRANSPORT_IMPLEMENTATION.md` documents the backend Tuya live remote-unlock transport behind the existing command guard. Default simulated mode and Tuya dry-run mode remain disabled; no physical site trial was authorized or executed. Remote lock, temporary PIN, frontend UI, and Raw API Tester remain out of scope.

M13F-C4 Go/No-Go decision: `13-smart-lock/SMART_LOCK_LIVE_SITE_TRIAL_GO_NO_GO_DECISION.md` records the readiness decision for the first controlled live `remote_unlock` site trial — **CONDITIONAL GO** for scheduling/preparation, **NO-GO** for live execution until all approvals, credential rotation confirmation, device readiness, and site dry-run evidence are signed off. Documentation only; no live command was executed and live commands remain disabled.

M13F-D freeze: `13-smart-lock/SMART_LOCK_READY_FOR_SITE_TRIAL_EXECUTION_PENDING_FREEZE.md` closes M13F as **"Ready for controlled site trial preparation, execution pending"** — backend technical foundation ready, local/placeholder C3-class dry-run evidence recorded (`artifacts/m13f-c4-site-evidence-pack/`, B-23 partially closed), live execution remains NO-GO, and the next allowed milestone is M13F-C5 (real site preflight + one-device live trial) only after all approvals and site-env evidence are complete. Documentation only; no live command was executed and `SMART_LOCK_LIVE_ENABLED` remains `false`.

M14D internal demo script: `14-production-readiness/INTERNAL_DEMO_SCRIPT_REFRESH.md` provides the safe, repeatable internal demo script based on M14A/M14B/M14C evidence — demo principles, pre-demo checklist, timed segment flow (A–K) with presenter narration in Bahasa Indonesia, payment proof and complaint attachment scripts, constrained Smart Lock wording (simulated/read-only/guarded-disabled only; live execution remains NO-GO), security talking points, fallback plan, and artifact references. Documentation only; no code changes, no QA executed, no live Smart Lock command.

M14F release readiness verdict: `14-production-readiness/RELEASE_READINESS_VERDICT.md` closes the M14 track — **Internal Demo Release: READY**, **Production Release: NOT READY**, **Smart Lock Live Execution: NO-GO / site trial pending** ("ready for controlled site trial preparation, execution pending"). Includes evidence basis (M14A–M14E + M13F-C4.1), blockers by severity, release decision matrix, approved internal demo scope, pre-production checklist, Smart Lock site-trial checklist, and recommended next milestones (M15A/M15B/M13F-C5/M15C). Documentation/decision only; no code changes, no QA executed, no live Smart Lock command.

M15A internal demo delivery package: `15a-stakeholder-demo/INTERNAL_DEMO_DELIVERY_PACKAGE.md` prepares the stakeholder review session — demo objectives, safe scope (per M14C/M14D), 30–45 minute agenda, presenter checklist, stakeholder feedback form, decision log template, binding Smart Lock stakeholder wording (site-trial pending; live execution NO-GO), evidence references, post-demo actions (M15B / M13F-C5 / M15C paths), and risk/fallback rules. Documentation only; no code changes, no QA executed, no live Smart Lock command; production NOT marked ready.

M15C-A payment gateway architecture freeze: `15c-payment-gateway/PAYMENT_GATEWAY_ARCHITECTURE_FREEZE.md` freezes the payment gateway product/architecture before implementation — provider-agnostic `PaymentGatewayProvider` interface with **Midtrans Sandbox** as the first adapter, invoice/payment-transaction lifecycle, **webhook as the backend source of truth** for paid status (signature validation, idempotency, replay safety), manual payment proof (M12) preserved as fallback, frontend UX contract aligned with `mockup/` and existing Admin/Penghuni patterns, security/env rules (server key backend-only, `PAYMENT_GATEWAY_ENABLED=false` default), DB planning (`payment_transactions`, optional `webhook_events`), normalized error codes, audit events, QA strategy, and implementation milestones M15C-B..G. Documentation only; no code implemented, no real credentials, payment gateway NOT production-ready.

M15C-B Midtrans provider contract freeze: `15c-payment-gateway/MIDTRANS_PROVIDER_CONTRACT_FREEZE.md` freezes the exact Midtrans Sandbox provider contract before backend implementation (M15C-C) — **Snap decision** (backend-generated Snap token / hosted payment page; server key never reaches frontend), `PaymentGatewayProvider` method contract (`createPaymentSession`, `parseAndVerifyWebhook`, `normalizeStatus`, optional `getPaymentStatus`, `capabilities`), provider order ID format `KST-{invoiceIdShort}-{attemptSeq}-{randomSuffix}` (≤ 50 chars, no PII), webhook verification contract (`POST /api/v1/payment-gateways/midtrans/webhook`, SHA512 `signature_key` validation, unknown-order/amount/currency mismatch → requires_review, never auto-paid), Midtrans status mapping table (capture+accept/settlement → paid; challenge/refund/chargeback/unknown → requires_review; paid terminal, no regression), idempotency/replay contract (dedupe key, atomic paid transition, single active attempt, session reuse), backend API contract to frontend (`POST /my/invoices/:invoiceId/payment-sessions`, `GET /my/invoices/:invoiceId/payment-status`, admin payment-transactions routes; provider-neutral responses, no secrets/raw payloads), env/error/audit contracts, frontend/mockup contract, manual proof compatibility (fallback preserved, paid-guard), QA acceptance criteria M15C-C..F, and the M15C-C implementation readiness checklist. Documentation/contract only; no code implemented, no real credentials, payment gateway NOT production-ready, architecture NOT locked to Midtrans.

M15C-C backend payment gateway foundation: `15c-payment-gateway/BACKEND_PAYMENT_GATEWAY_FOUNDATION.md` documents the backend implementation — `payment_transactions` and `payment_webhook_events` tables, payment gateway module/service/repository/controllers, fail-closed env defaults, Midtrans provider skeleton behind a provider interface, resident/admin endpoints, manual proof paid-invoice guard, validation results, and remaining M15C-D webhook work. No frontend UI, no real Midtrans credentials, no live provider call, and no invoice auto-paid by gateway.

M15C-D Midtrans Sandbox Snap + webhook settlement: `15c-payment-gateway/MIDTRANS_SANDBOX_SNAP_WEBHOOK_SETTLEMENT.md` documents the backend-only implementation of real Midtrans Sandbox Snap session creation, signed webhook verification, status mapping, idempotent invoice settlement, manual proof compatibility, security/no-secret boundaries, validation results, known limitations, and M15C-E next steps. No frontend UI was implemented and Payment Gateway remains not production-ready.

M15C-E1 frontend payment UX plan: `15c-payment-gateway/FRONTEND_PAYMENT_CTA_ADMIN_STATUS_UI_PLAN.md` defines the frontend UX contract and implementation plan for M15C-E2 — binding frontend rules (never paid-from-redirect; always refresh/poll backend status; CTAs hidden when paid; server key never in frontend; provider-neutral wording), the Penghuni billing state machine (unpaid → pending → return → paid / failed-expired-cancelled / requires_review), API integration using the existing M15C-C/D endpoints, paymentUrl-first Snap open behavior with conditional Snap.js/client-key decision, bounded polling + manual "Cek Status", Admin payment transaction table/detail with Gateway/Manual source badges ("Terkonfirmasi Otomatis" for gateway-paid rows; verify/reject stays on manual proofs; "Perlu Tinjauan" for requires_review), frozen Indonesian UX copy, error→UX mapping, likely implementation file mapping for `apps/penghuni` / `apps/admin` (with `packages/api-client` frozen per ADR-FE-001), the M15C-F QA plan, and open questions. Planning/documentation only; no frontend/backend code implemented, no browser QA executed, manual payment proof preserved as fallback, Payment Gateway remains staging/sandbox and NOT production-ready.

M15C-E2A/E2B frontend payment implementation and technical validation: `15c-payment-gateway/FRONTEND_PAYMENT_CTA_ADMIN_STATUS_UI_IMPLEMENTATION.md` documents the frontend source implementation per the M15C-E1 plan — Penghuni `OnlinePaymentCard` (states A–F: "Bayar Online" CTA, "Menunggu Pembayaran Online", post-return confirming copy, "Lunas" + source label, retry on failed/expired, "Perlu Tinjauan"), `usePaymentGateway` hooks (create session with idempotency key, payment-status query with bounded polling 5 s → 15 s ≤ ~5 menit, safe error copy mapping), paymentUrl same-tab opening (Snap.js intentionally not enabled; no client key env added), manual payment proof preserved as fallback and hidden when paid; Admin "Online" tab (`usePaymentTransactions` hooks, gateway transaction table + safe detail dialog, badge "Gateway"/"Terkonfirmasi Otomatis"/"Perlu Tinjauan", no verify/reject for gateway rows, 403 → ForbiddenState). Codex validation covered lint/typecheck/build for Penghuni/Admin and API smoke for payment session/status/admin transaction/manual-proof guard; no server key/client key/raw provider payload in frontend. Payment Gateway remains NOT production-ready.

M15C-F/F2 payment gateway sandbox E2E QA: `15c-payment-gateway/PAYMENT_GATEWAY_SANDBOX_E2E_QA.md` records the VPS staging QA run — deployment freshness/static validation/health/build/leakage checks passed, Hybrid Manual Browser QA confirmed Penghuni and Admin UI behavior, Midtrans Sandbox Snap opened from the UI, signed webhook simulation settled the invoice, duplicate/idempotency and invalid-signature checks passed, and manual proof paid-guard remained intact. Payment Gateway remains sandbox/staging only and NOT production-ready.

M15C-G payment gateway release/documentation update: `15c-payment-gateway/PAYMENT_GATEWAY_RELEASE_UPDATE.md` closes the M15C track — **Payment Gateway sandbox/staging ready; Midtrans Sandbox validated; production payment activation pending; Payment Gateway is not production-ready**. Webhook is the source of truth; redirect is UX only; manual payment proof remains fallback. Governance docs refreshed (`PROJECT_MASTER.md`, `ROADMAP.md`, `CHANGELOG.md`, `PROJECT_HANDOFF.md`, `INTERNAL_DEMO_CHECKLIST.md` Section 14, this index). Documentation only; no code changes, no credentials, no QA executed, Smart Lock status unchanged (site trial pending; live execution NO-GO), production remains NOT READY.

## File Upload, Payment Proof, dan Complaint Attachment (M12)

Seluruh dokumen milestone M12 berada di `12-product-readiness/`:

| Topik | Dokumen |
| --- | --- |
| ADR akses file backend-mediated (binding) | `01-architecture/ADR-BE-FILE-001_BACKEND_MEDIATED_FILE_ACCESS.md` |
| Mockup feature gap audit (M12A) | `12-product-readiness/MOCKUP_FEATURE_GAP_AUDIT.md` |
| Feature flag / placeholder hardening (M12B) | `12-product-readiness/FEATURE_FLAG_PLACEHOLDER_HARDENING.md` |
| Rencana arsitektur File Upload (M12C) | `12-product-readiness/FILE_UPLOAD_FOUNDATION_PLAN.md` |
| Implementasi Backend File API (M12C1) | `12-product-readiness/FILE_UPLOAD_FOUNDATION_IMPLEMENTATION.md` |
| Rencana generic upload engine frontend (M12C2) | `12-product-readiness/GENERIC_UPLOAD_ENGINE_PLAN.md` |
| Implementasi generic upload engine frontend (M12C2) | `12-product-readiness/GENERIC_UPLOAD_ENGINE_IMPLEMENTATION.md` |
| Penghuni Manual Payment Proof Upload (M12C3) | `12-product-readiness/MANUAL_PAYMENT_PROOF_UPLOAD_IMPLEMENTATION.md` |
| Complaint Attachment Backend Readiness (M12C4) | `12-product-readiness/COMPLAINT_ATTACHMENT_BACKEND_READINESS.md` |
| Admin File Preview / Review (M12C5) | `12-product-readiness/ADMIN_FILE_PREVIEW_REVIEW_IMPLEMENTATION.md` |
| Penghuni Complaint Create UI + Attachment (M12D) | `12-product-readiness/PENGHUNI_COMPLAINT_CREATE_ATTACHMENT_IMPLEMENTATION.md` |

## Prinsip Arsitektur (Ringkas)

- Backend adalah titik penegakan kebijakan final. Validasi frontend bersifat UX-only.
- PostgreSQL adalah system of record. Redis hanya untuk runtime/cache/queue/rate-limit.
- Property scoping wajib. Resident self-scope ditegakkan oleh backend.
- Tidak ada URL file publik. Preview file menggunakan akses terotorisasi yang dimediasi backend. `storage_path` tidak pernah diekspos ke frontend.
- Tidak didukung pada fase ini: video upload, chat attachment.
- Payment Gateway (M15C): **sandbox/staging ready** — Midtrans Sandbox validated; **production payment activation pending**; Payment Gateway is not production-ready. Webhook is the source of truth; redirect is UX only; manual payment proof remains fallback.
- Milestone mendatang: receipt/nota, payment production activation readiness, Smart Lock live site trial (execution pending), CCTV live.
